import React from "react";

const Failure = () => (
  <div id="failure" className="after-submit">
    <p>Something went wrong.</p>
    <p>Please refresh and try again.</p>
  </div>
);
export default Failure;